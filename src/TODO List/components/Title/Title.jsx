import React from 'react';

function Title({children}) {
  return (            
    <section
        style={{ color: 'red',
         }}
         >
        {children}
    </section>
  );
};

export default Title;